if(jibu.layout.applike.MainPanel) {
    Ext.override(jibu.layout.applike.MainPanel, {
        accountText:'Account',
        accountTooltip:'Account Setting',
        exitText:'Exit',
        centerPanelText:'Welcome'
    });
}

if(jibu.layout.applike.startpanel) {
    Ext.override(jibu.layout.applike.startpanel, {
        mainMenuText:'Start'
    });
}
